def bombelki(n):
    """
    This functions takes an integer and prints that many bubbles as you specified.
    No returns
    """
    if type(n) == int:
        bombelki = n * "o"
        print(bombelki)